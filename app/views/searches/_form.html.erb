<%= form_tag(search_path, method: :get) do %>
  <%= text_field_tag (:word) %>
  <%#= f.select :name, [["User", User.all]], include_blank: "選択して下さい" %>
  <%#= select_tag "submit_name", options_for_select([["User", "name"],["Book","title"]]), include_blank: "選択して下さい" %>

  <%= select_tag 'range' ,options_for_select([['User', '1'], ['Book', '2']]) %>
  <%#= select_tag "submit_name", options_for_select([["完全一致", "User"],["前方一致","前方一致"],["後方一致","後方一致"],["部分一致","部分一致"]]), include_blank: "選択して下さい" %>
  <%= select_tag 'search', options_for_select([["前方一致","forward_match"], ["後方一致","backward_match"], ["完全一致","perfect_match"], ["部分一致","partial_match"]]) %>
  <%= submit_tag :検索 %>
<% end %>

<%#= form_with url: search_books_path, method: :get, local: true do |f| %>
  <%#= f.text_field :title %>
  <%#= f.select :title, :body, [["Book", Book.all]], include_blank: "選択して下さい" %>
  <%#= f.submit :search %>
<%# end %>